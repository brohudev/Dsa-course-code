#include <iostream>
#include <string>
#include <algorithm>
#include <fstream>
#include <vector>
using namespace std;
//! create a dynamic array of pointers to nodes.
//! randomely generate a number
//! every element of the array is a pointer to the node, and every number is added to the beginning of the ll.
     //! every number's index is found by num % 10. this gives a dynamic array of 10 
//! google the cashing techniques for your processors.

//linear hashing quadratic probing. 
//open chaining is on thursday for the first 10 mins. ?
int main(int argc, char* argv[]){
     vector<vector<int>> array;
     int **array = new int*[10];
     
     return 0;
}