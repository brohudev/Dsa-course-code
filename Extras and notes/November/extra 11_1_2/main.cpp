#include <iostream>
#include <string>
#include <algorithm>
#include <fstream>

using namespace std;

int main(int argc, char* argv[]){
     //!http://replit.com/@nouhad2007/treetracersal
     //* postorder preorder and inorder traversal of heaps.

     //* preorder traversal is basically the cout but before the recursion calls.
     //* inorder is basically cout in between calls.
     //* 
     return 0;
}